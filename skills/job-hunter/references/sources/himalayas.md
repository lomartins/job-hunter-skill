# Source: Himalayas

HTML with a GraphQL endpoint behind the scenes. Discoverable via browser devtools network tab.

## Endpoints

- Listing: `https://himalayas.app/jobs?category=engineering`
- GraphQL: `https://himalayas.app/api/graphql` — query the network tab while filtering to capture the latest query shape (it drifts).

## Phase 1 scope

Stub. **Phase 3** (lower priority). Document the captured GraphQL query in this file before implementing.
