# Source: Arc.dev

Curated international remote dev jobs. Full listing requires auth.

## Endpoint

- Listing (public preview): `https://arc.dev/remote-jobs?role=mobile-engineer`
- Detail: needs `arc.dev` session cookie.

## Auth

Capture the `arc.session` cookie into `personal.env` as `ARC_SESSION=...`.

## Phase 1 scope

Stub. **Phase 3** (lowest priority).
